/* eslint-disable no-undef */
// const productMutations = require("../resolvers/mutations/product.mutations");
// const productQueries = require("../resolvers/queries/product.queries");

beforeAll(() => {
  console.log("Before all tests categories");
  // queries = productQueries(global.models);
  // mutations = productMutations(global.models);
});

describe("-- Categories Queries --", () => {
  // let mutations = null;
  // let queries = null;

  test("getPCategories", () => {
    // const products = queries.getProducts();
    // console.log(mutations, global.models);
    // console.log("mutations DK", global.models);

    // console.log(products);
  });
});
